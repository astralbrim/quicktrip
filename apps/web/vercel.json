{
  "version": 2,
  "buildCommand": "cd ../.. && pnpm install && cd packages/shared && pnpm build && cd ../../apps/api && pnpm prisma generate && cd ../web && pnpm build",
  "installCommand": "echo 'Skipping default install'",
  "builds": [
    {
      "src": "next.config.js",
      "use": "@vercel/next"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}