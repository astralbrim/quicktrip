{
  "version": 2,
  "buildCommand": "cd ../.. && npm install -g pnpm && pnpm install && cd packages/shared && pnpm build && cd ../../apps/api && pnpm prisma generate && cd ../web && pnpm build",
  "installCommand": "echo 'Skip npm install, using pnpm in buildCommand'",
  "builds": [
    {
      "src": "next.config.js",
      "use": "@vercel/next"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}